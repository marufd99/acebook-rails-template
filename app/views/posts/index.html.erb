<% @posts.reverse.each do |post| %>
  <p><%= post.user_id %>
    <!-- post.user -->
  <p><%= post.message %></p>
  <p><%= post.created_at.strftime("%d/%m/%Y") %></p>
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
